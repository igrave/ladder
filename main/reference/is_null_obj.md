# A helper function that tests whether an object is either NULL *or* a list of NULLs

A helper function that tests whether an object is either NULL *or* a
list of NULLs

## Usage

``` r
is_null_obj(x)
```
